# Repository defaults only. Reader-owned values are loaded from the root config/application-default.yml.
spring:
  ai:
    model:
      chat: openai
      embedding: none
    openai:
      api-key: replace-with-your-api-key
      base-url: https://api.openai.com/v1
      max-retries: 0
      chat:
        model: gpt-4.1-mini
