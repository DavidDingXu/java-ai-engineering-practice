# Knowledge Service and Ticket Agent Service import this shared model configuration.
# Replace the Chat API key. Full RAG uses the local Ollama model configured below.
# Never commit a real key. Company deployments must override secrets through their secret manager.
spring:
  ai:
    model:
      chat: openai
      embedding: none
    openai:
      api-key: replace-with-your-api-key
      base-url: https://api.openai.com/v1
      max-retries: 0
      chat:
        model: gpt-4.1-mini

java-ai:
  knowledge:
    embedding:
      mode: ollama
      ollama:
        base-url: http://localhost:11434
        model: qwen3-embedding:4b
        timeout-seconds: 120
